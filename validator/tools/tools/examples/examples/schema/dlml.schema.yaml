$schema: "http://json-schema.org/draft-07/schema#"
title: "DLML Schema"
type: object
required:
  - layer
properties:
  layer:
    type: string
    enum: ["meta", "context", "state", "intent", "payload"]
